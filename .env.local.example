NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key is unused in v1; reserved for future admin jobs. Never expose client-side.
SUPABASE_SERVICE_ROLE_KEY=
TMDB_READ_TOKEN=
# Canonical site origin for share links; falls back to the request host when unset.
NEXT_PUBLIC_SITE_URL=
